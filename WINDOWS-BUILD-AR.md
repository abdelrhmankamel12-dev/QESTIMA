# تجهيز بناء Windows — لم يُشغّل بعد

هذه حزمة مصدر وإعدادات بناء، وليست برنامج تثبيت جاهزًا. لا تعني اكتمال إصلاح جميع مشاكل QESTIMA.

أضيف مسار يدوي في `.github/workflows/windows-build.yml` لتجهيز Windows Server 2022 مع Node وElectron وNSIS وPlaywright، ثم تشغيل الاختبارات وبناء مثبت غير موقّع وتثبيته وتجربة فتحه وإنشاء حساب وإعادة التشغيل وتسجيل الدخول وفتح نافذة المشروع. الصور الناتجة صور تشغيل حقيقية عند نجاح التشغيل، وليست صورًا موجودة الآن.

## التشغيل

1. ضع محتويات هذا المجلد في جذر مستودع QESTIMA خاص على GitHub، بما فيها مجلد `.github`.
2. تأكد أن استخدام Actions يقع ضمن رصيدك المتاح وأن الإنفاق المدفوع غير مفعّل. الحزمة لا تتحقق من رصيد الحساب ولا تغيّر إعدادات الفوترة.
3. من Actions اختر Windows build and startup test ثم Run workflow.
4. بعد النجاح نزّل QESTIMA-Windows-Preview-Unsigned. صور التشغيل والسجلات في QESTIMA-Windows-test-evidence، وتُحفظ لمدة سبعة أيام.

لا يُرفع المثبت إذا فشل أحد الاختبارات. المثبت Preview-Unsigned وليس إصدارًا تجاريًا معتمدًا. المحاولة الأولى قد تكشف عيوبًا تتطلب تصحيحًا وإعادة التشغيل.

## حدود التحقق

تم التحقق محليًا من صياغة اختبار JavaScript وبنية YAML فقط، مع اختبارات المصدر. لم يتم تنزيل أدوات Windows أو تنفيذ PowerShell أو تشغيل هذه المهمة على Windows في هذه الجلسة. الاختبارات المقترحة لا تغطي جميع أوامر Ribbon أو حسابات القياس أو DWG أو التراخيص المركزية أو ترقية/إزالة البرنامج أو الملفات الكبيرة أو انقطاع الطاقة. بعض هذه الوظائف لا تزال غير مكتملة في المصدر.

بيئة الجلسة Linux بدون Wine أو Windows VM، والوصول المباشر إلى تنزيل Electron لم ينجح. المستودع الوحيد الظاهر في الاتصال الحالي هو مشروع غنيمة، ولم تُرفع إليه ملفات QESTIMA.

مراجع طريقة التنفيذ:
- https://playwright.dev/docs/api/class-electron
- https://docs.github.com/en/actions/tutorials/store-and-share-data
